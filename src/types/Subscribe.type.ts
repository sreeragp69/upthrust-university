export interface SubscribeProps {
    isLoading?: boolean
  }
  
  export interface SubscribeFormData {
    email: string
  }
  
  export interface SubscriberStats {
    count: string
    label: string
    avatars: string[]
  }